enum authData {
  email = 'email',
  username = 'username',
  password = 'password',
  confirmPassword = 'confirmPassword'
}

enum authFormType {
  login = 'login',
  register = 'register'
}

export { authData, authFormType }
