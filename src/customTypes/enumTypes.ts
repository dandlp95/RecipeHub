enum authData {
  username = 'username',
  password = 'password',
  email = 'email'
}

enum authFormType {
    login = 'login',
    register = 'register' 
}

export { authData, authFormType }
