enum authData {
  email = 'emailAddress',
  username = 'userName',
  password = 'password',
  confirmPassword = 'confirmPassword'
}

enum authFormType {
  login = 'login',
  register = 'register'
}

export { authData, authFormType }
